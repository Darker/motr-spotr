Wire calculation:

(0.050)/((0.00070*0.5)**2 * 8960 * Math.PI) 

Turns calculation:
```
wire_len / (Math.PI*2*(antenna_dia + ridge_thick)*0.5) =
 = wire_len / Math.PI * (210.75*0.001 + 3*0.001)
 = 

```